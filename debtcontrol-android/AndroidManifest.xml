<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kalandyk"
    android:versionCode="1"
    android:versionName="1.0.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:debuggable="true"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar">

        <!--<service android:name=".android.scheduler.SchedulerEventService" android:process=":hascode_process">-->
        <!--</service>-->

        <!--<receiver android:name=".android.scheduler.SchedulerEventReceiver" android:process=":hascode_process">-->
        <!--</receiver>-->
        <!--<receiver android:name=".android.scheduler.SchedulerSetupReceiver" android:process=":hascode_process">-->
            <!--<intent-filter >-->
                <!--<action android:name="android.intent.action.BOOT_COMPLETED" />-->
                <!--<action android:name="android.intent.action.USER_PRESENT" />-->
            <!--</intent-filter>-->
        <!--</receiver>-->

        <activity
            android:name=".android.activities.MainActivityDebt"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>